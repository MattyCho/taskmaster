type Task @model {
    id: ID!
    taskTitle: String
    taskDescription: String
    taskStatus: TaskStatusEnum
    taskDate: AWSDateTime
}

enum TaskStatusEnum {
    NEW,
    ASSIGNED,
    IN_PROGRESS,
    COMPLETE
}